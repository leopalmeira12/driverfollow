# ==================================
# TubeDrivers Environment Variables
# ==================================

# Server
PORT=5000
NODE_ENV=development

# MongoDB (opcional - funciona em modo memória sem isso)
MONGODB_URI=mongodb://localhost:27017/driverfollow

# JWT Secret (mude em produção!)
JWT_SECRET=sua_chave_secreta_muito_segura_aqui

# Frontend URL (para redirecionamento OAuth)
FRONTEND_URL=http://localhost:5173

# ==================================
# GOOGLE OAUTH CONFIGURATION
# ==================================
# Siga estes passos para configurar:
#
# 1. Acesse: https://console.cloud.google.com/
# 2. Crie um novo projeto ou selecione existente
# 3. Vá em "APIs & Services" > "OAuth consent screen"
#    - User Type: External
#    - App name: TubeDrivers
#    - User support email: seu email
#    - Authorized domains: deixe vazio para localhost
# 4. Vá em "APIs & Services" > "Credentials"
#    - Create Credentials > OAuth 2.0 Client IDs
#    - Application type: Web application
#    - Name: TubeDrivers Web Client
#    - Authorized redirect URIs: http://localhost:5000/api/auth/google/callback
# 5. Copie o Client ID e Client Secret abaixo
# 6. Vá em "APIs & Services" > "Library"
#    - Habilite: "YouTube Data API v3"
#
# ==================================

GOOGLE_CLIENT_ID=seu_google_client_id_aqui
GOOGLE_CLIENT_SECRET=seu_google_client_secret_aqui
GOOGLE_REDIRECT_URI=http://localhost:5000/api/auth/google/callback
